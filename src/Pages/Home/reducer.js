// const EXAMPLE_TYPE = 'EXAMPLE_TYPE';

// export function handleSearch(name = 'location', value) {
//   return {
//     type: HANDLE_SEARCH,
//     payload: { name, value }
//   };
// }

const initialState = {};

export default function(state = initialState, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}
